@page
@model MarketPlace.Pages.Admin.MessageTicket.IndexModel

@{
    ViewData["Title"] = "صفحه اصلی";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}


<section class="main_content_area">
    <div class="container">
        <div class="account_dashboard">
            <div class="row">
                <div class="col-sm-12 col-md-3 col-lg-3">
                    @await Component.InvokeAsync("SideBarAdmin")
                </div>

                <div class="col-sm-12 col-md-9 col-lg-9">
                    <!-- Tab panes -->
                    <div class="tab-content dashboard_content">

                    
                        <p>
                            <a asp-page="Create">ایجاد کردن</a>
                        </p>
                        <div>
                            <div>
                                <table>
                                    <thead>
                                        <tr>
                                            <th scope="col">
                                                @Html.DisplayNameFor(model => model.TicketMessage[0].Text)
                                            </th>
                                            <th>
                                                @Html.DisplayNameFor(model => model.TicketMessage[0].Ticket)
                                            </th>
                                            <th scope="col">
                                                فرستنده
                                            </th>
  
                                            <th></th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                   
                                        @foreach (var item in Model.TicketMessage)
                                        {
                                            <tr>
                                                <td scope="row" data-label="@Html.DisplayNameFor(model => model.TicketMessage[0].Text)">@Html.DisplayFor(modelItem => item.Text)</td>
                                                <td data-label="@Html.DisplayNameFor(model => model.TicketMessage[0].Ticket)">@Html.DisplayFor(modelItem => item.Ticket.Title)</td>
                                                <td data-label="فرستنده">@Html.DisplayFor(modelItem => item.Sender.LastName)</td>
                                                <td>
                                                    <a asp-page="./Edit" asp-route-id="@item.Id">ویرایش</a> |
                                                    <a asp-page="./Details" asp-route-id="@item.Id">جزئیات</a> |
                                                    <a asp-page="./Delete" asp-route-id="@item.Id">حذف</a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
